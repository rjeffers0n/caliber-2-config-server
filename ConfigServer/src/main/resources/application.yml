server:
  port: 8888
  
eureka:
  client:
    service-url:
      default: ${EUREKA_HOME}
  instance:
    lease-expiration-duration-in-seconds: 2
    lease-renewal-interval-in-seconds: 1

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/August112641/config-server.git