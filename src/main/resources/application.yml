server:
  port: 8888
 
eureka:
 client:
   service-url:
     default: http://caliber-2-dev-alb-315997072.us-east-1.elb.amazonaws.com/eureka/eureka 
 instance:
   lease-expiration-duration-in-seconds: 2
   lease-renewal-interval-in-seconds: 1

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/revaturelabs/caliber-2-central-config-repo.git
          
          